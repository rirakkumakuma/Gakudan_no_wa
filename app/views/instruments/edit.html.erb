<h1>楽器パート編集ページ</h1>
<%= form_with model: @instrument, url: "/instruments/#{@instrument.id}", local: true do |f| %>
<label for="instrument_name">パート名</label><br>
  <%= f.text_field :name %>
<%= f.submit '変更を保存' %>
<% end %>